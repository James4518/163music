export interface IBanner {
  imageUrl: string;
  titleColor: string;
  typeTitle: string;
  url: string;
}
export interface IHotRecommends {
  id: number;
  name: string;
  picUrl: string;
  playCount: number;
  trackCount: number;
  highQuality: boolean;
}
